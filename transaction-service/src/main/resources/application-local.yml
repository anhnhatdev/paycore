spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/paycore_transaction
    username: paycore
    password: paycore123
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.paycore.transactionservice: DEBUG
    org.springframework.web: INFO
    org.springframework.kafka: INFO
